{
  "$id": "types/MAPI_PATROL_GIMBAL_ACTION_PARAMETERS.schema.json",
  "x-stoplight": {
    "id": "174e50ecbc205"
  },
  "allOf": [
    {
      "additionalProperties": false,
      "type": "object",
      "properties": {
        "yaw": {
          "type": "number",
          "description": "Desired gimbal yaw angle (degrees): measured clockwise from true north when relType is `North`, or clockwise from the upcoming leg’s heading when relType is `FlightLeg`.",
          "x-stoplight": {
            "id": "uvp5pbcum6fdl"
          }
        },
        "viewDistance": {
          "type": "number",
          "description": "Horizontal range to the view target, in meters; combined with the drone’s current altitude and the target’s ground elevation to compute the required gimbal pitch.",
          "x-stoplight": {
            "id": "5p3775hgsr34q"
          }
        },
        "viewDuration_ms": {
          "type": "number",
          "x-stoplight": {
            "id": "x3nnidszej36h"
          },
          "description": "timeout for first `gimbalMove` action (enter `gimbalMove`). The gimbal will be pointed at the point for at least the specified time (`duration_ms`)\nIf this field is not defined, the value will be 0"
        },
        "relType": {
          "type": "string",
          "enum": [
            "Body"
          ],
          "description": "Reference frame for the yaw angle. Currently only `Body` (relative to the drone's body heading) is supported for gimbalMove actions."
        },
        "isAiAction": {
          "type": "boolean",
          "description": "When `true`, the captured frame is sent to the AI module specified by `aiModule` for analysis."
        },
        "aiModule": {
          "type": "string",
          "enum": [
            "Traffic",
            "License Plate",
            "Prompt Based"
          ],
          "description": "AI module to use for analysis: `Traffic` for vehicle counting/occupancy, `License Plate` for plate recognition, or `Prompt Based` for custom prompt-driven detection."
        },
        "trafficType": {
          "type": "string",
          "enum": [
            "Traffic Count",
            "Occupancy"
          ],
          "description": "Sub-mode for the `Traffic` AI module: `Traffic Count` counts vehicles, `Occupancy` measures area occupancy."
        },
        "selectedCube": {
          "type": "object",
          "description": "References a pre-configured detection cube for AI analysis.",
          "properties": {
            "cubeName": {
              "type": "string"
            },
            "cubeId": {
              "type": "number"
            }
          },
          "additionalProperties": false
        },
        "prompt": {
          "type": "string",
          "description": "Free-text prompt sent to the `Prompt Based` AI module describing what to detect or analyze."
        },
        "detectionType": {
          "type": "string",
          "enum": [
            "detect",
            "query",
            "point"
          ],
          "description": "Detection strategy for the `Prompt Based` AI module: `detect` finds objects, `query` answers questions about the scene, `point` identifies a specific location."
        },
        "alertAction": {
          "type": "boolean",
          "description": "When `true`, triggers an alert if the AI module detects a match."
        },
        "watchObjects": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of object labels the AI module should watch for (e.g. `[\"person\", \"vehicle\"]`)."
        }
      },
      "required": [
        "yaw",
        "viewDistance"
      ]
    },
    {
      "$ref": "./MAPI_ACTION_ZOOM.json",
      "x-stoplight": {
        "id": "rsiskgnkxu248"
      }
    }
  ],
  "x-vendored-from": {
    "repo": "FlightOps-Mission-API",
    "path": "Models/Mission/Patrol/MAPI_PATROL_GIMBAL_ACTION_PARAMETERS.json",
    "date": "2026-06-01"
  }
}
